<div id="content">
	<div class="back-nav"><a href="#"><span>&larr;</span> обратно в магазин</a></div>

	<div class="brand-desc">
		<h2><%= @brand.title %></h2>
		<div class="brand-logo"><%= image_tag @brand.image.url, :alt => @brand.title %></div>		
		<div><%= @brand.body.gsub(/\n/, "<br />") %>
		</div>
	</div>
	
</div>